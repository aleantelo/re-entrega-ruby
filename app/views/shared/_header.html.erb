<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <% if current_user %>
        <%= link_to "<span class='nav-link'><img src='https://icongr.am/fontawesome/home.svg?size=26&color=currentColor'></span>".html_safe, '/' %>   
    <% end %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavDropdown">
    <ul class="navbar-nav">  
     <% if signed_in? && current_user.rol == "Cliente"%>
        <li class="nav-item">
            <%= link_to "<span class='nav-link'>Sacar turno</span>".html_safe, sucursals_path %>
        </li>
        <li class="nav-item">
            <%= link_to "<span class='nav-link'>Ver turnos</span>".html_safe, turnos_path %>
        </li>
    <% end %>
    <% if signed_in? && current_user.rol == "Admin" %>
    <li class="nav-item">
        <%= link_to "<span class='nav-link'>Ver usuarios</span>".html_safe, usuarios_path %>
    </li>
    <li class="nav-item">
        <%= link_to "<span class='nav-link'>Ver sucursales</span>".html_safe, sucursals_path %>
    </li>
    <% end %>
    <% if signed_in? && current_user.rol == "Personal Bancario" %>
        <li class="nav-item">
            <%= link_to "<span class='nav-link'>Ver sucursales</span>".html_safe, sucursals_path %>
        </li>
        <li class="nav-item">
            <%= link_to "<span class='nav-link'>Ver turnos</span>".html_safe, turnos_path %>
        </li>
    <% end %>
    </ul>
  </div>
  <div>
    <ul class="navbar-nav">
        <% if not signed_in? %>
            <li class="nav-item active">
                <%= link_to "<span class='nav-link'>Iniciar sesión</span>".html_safe, new_user_session_path %>
            </li>
            <li class="nav-item active">
                <span class='nav-link'>|</span>
            </li>
            <li class="nav-item active">
                <%= link_to "<span class='nav-link'>Registrarse</span>".html_safe, new_user_registration_path %>
            </li>
        <% end %>
        <% if current_user%>
          <li class="nav-item">
              <span><%= link_to "<span class='nav-link'>Perfil</span>".html_safe, usuario_path(current_user.id, ) %></span>
          </li>
        <% end %>
        <% if signed_in? %>
        <li class="nav-item">
            <%= link_to "<span class='nav-link'>Cerrar sesión</span>".html_safe, destroy_user_session_path %>
        </li>
        <% end %>  
        </ul>
    <div>
</nav>
<hr>
